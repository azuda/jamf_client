[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "jamf_client"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "python-dotenv",
    "urllib3",
]

[project.optional-dependencies]
truststore = ["truststore"]
